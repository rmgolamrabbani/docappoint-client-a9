export const metadata = {
  title: "Profile",

  description:
    "View and manage your profile information on DocAppoint.",
};

export default function ProfileLayout({ children }) {
  return children;
}