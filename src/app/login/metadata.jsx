export const metadata = {
  title: "Login",

  description:
    "Sign in to your DocAppoint account to access your appointments and healthcare services.",
};

export default function LoginLayout({ children }) {
  return children;
}